# Node
NODE_ENV=development
PORT=3000

# Database
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASS=password
DB_NAME=database_name

# Mongo (if using Mongo)
DB_URI=mongodb://localhost:27017/blog

# SMTP / Mailer
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=secret
SMTP_FROM="My App <no-reply@example.com>"
SMTP_SECURE=false